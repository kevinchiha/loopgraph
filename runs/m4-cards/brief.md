# M4 brief

Add a `--farewell` flag to `cli.py` that prints exactly `farewell from toy`,
plus a test in `test_cli.py` proving it (same style as existing tests).

Done when `python -m pytest -x -q` passes.
