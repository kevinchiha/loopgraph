# M2 toy brief

Add a `--hello` flag to `cli.py` that prints exactly `hello from toy`, plus a test
in `test_cli.py` proving it (same style as the existing test).

Done when `python -m pytest -x -q` passes in the worktree.
