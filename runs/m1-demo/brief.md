# M1 gate proof

Deliberately failing gate to prove a red gate blocks the workflow and shows up
`red` in the status query. `gates.yaml` is all-green (control);
`gates.red.yaml` adds the planted failure.
