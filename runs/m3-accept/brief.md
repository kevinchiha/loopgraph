# M3 accept-control brief

Add a `--goodbye` flag to `cli.py` that prints exactly `goodbye from toy`,
plus a test in `test_cli.py` proving it (same style as the existing tests).

Done when `python -m pytest -x -q` passes.
